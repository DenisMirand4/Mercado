import * as React from 'react';
import { Text, View } from 'react-native';

const EscanerScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Tela de Scanner</Text>
        </View>
    );
}

export default EscanerScreen;